<%= provide(:title, 'Usuarios') %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.empresa, user %>
    </li>
  <% end %>
</ul>
<%= will_paginate @users %>

<%= link_to 'Nuevo usuario', new_user_path %>
